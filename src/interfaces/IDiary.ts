export interface IDiary {
  situation: string;
  think: string;
  emotion: string;
  reaction: string;
  action: string;
  date: Date;
}

export interface IDiaryListViewDTO {
  email: string;
}
